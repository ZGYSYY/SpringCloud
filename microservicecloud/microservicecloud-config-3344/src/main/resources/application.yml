server:
  port: 3344
spring:
  application:
    name: microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:ZGYSYY/SpringCloud.git #git仓库地址
          search-paths: cloud-conf