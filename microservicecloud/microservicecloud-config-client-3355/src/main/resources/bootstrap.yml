spring:
  cloud:
    config:
      # github上配置文件的名称，但是不加yml后缀
      name: microservicecloud-config-client
      # 本次访问的配置项
      profile: test
      # GitHub上的项目分支
      label: master
      # 本微服务启动后，先去找配置中心服务端，通过springCloudConfig获取GitHub上的服务地址
      uri: http://myconfig3344.com:3344